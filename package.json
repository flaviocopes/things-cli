{
  "name": "things-cli",
  "version": "0.1.0",
  "description": "Command line tool for the Things 3 app on macOS",
  "type": "module",
  "bin": {
    "things": "./bin/things.js"
  },
  "scripts": {
    "check": "node --check bin/things.js && node --check lib/*.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
